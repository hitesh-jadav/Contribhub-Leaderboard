<!DOCTYPE html>
<html>
<head>
  <title>My Profile</title>
  <style>
    body { font-family: Arial; padding: 20px; background: #f5f5f5; }
    .card { background: white; padding: 20px; border-radius: 10px; max-width: 600px; margin: auto; box-shadow: 0 0 10px rgba(0,0,0,0.1); }
    img { border-radius: 50%; width: 100px; }
    h2 { margin-top: 0; }
    .section { margin-bottom: 20px; }
    .section h3 { margin-bottom: 5px; border-bottom: 1px solid #ddd; }
  </style>
</head>
<body>
  <div class="card">
    <h2>👤 <%= user.name %>'s Profile</h2>

    <div class="section">
      <h3>📧 Email</h3>
      <p><%= user.email %></p>
    </div>

    <% if (github) { %>
    <div class="section">
      <h3>🐙 GitHub</h3>
      <img src="<%= github.avatar_url %>" alt="GitHub Avatar" />
      <p><strong>Username:</strong> <%= github.username %></p>
      <p><strong>Followers:</strong> <%= github.followers %></p>
      <p><strong>Following:</strong> <%= github.following %></p>
      <p><strong>Public Repos:</strong> <%= github.public_repos %></p>
      <p><a href="<%= github.profile_url %>" target="_blank">View on GitHub</a></p>
    </div>
    <% } %>

    <% if (stackoverflow) { %>
    <div class="section">
      <h3>📚 StackOverflow</h3>
      <img src="<%= stackoverflow.profile_image %>" alt="SO Avatar" />
      <p><strong>Name:</strong> <%= stackoverflow.display_name %></p>
      <p><strong>Reputation:</strong> <%= stackoverflow.reputation %></p>
      <p><strong>Badges:</strong> 🥇<%= stackoverflow.badges.gold %>, 🥈<%= stackoverflow.badges.silver %>, 🥉<%= stackoverflow.badges.bronze %></p>
      <p><a href="<%= stackoverflow.link %>" target="_blank">View on StackOverflow</a></p>
    </div>
    <% } %>
  </div>
</body>
</html>
